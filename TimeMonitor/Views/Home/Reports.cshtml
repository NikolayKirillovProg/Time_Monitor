@model TimeMonitor.ViewModels.ReportViewModel
@{
    ViewBag.Title = "Reports";
}
<br />
<h2>Отчеты</h2>
<br />
<div>
    <table>
        <tr>
            <td>
                @Html.ActionLink("Пользователи", "Index", "Home")
            </td>
        </tr>
    </table>
</div>
<br />
@using (Html.BeginForm("UserReports", "Home", FormMethod.Post, new {@report = Model }))
{
    @Html.DropDownListFor(m => m.User_id, ViewBag.UserList as SelectList)@Html.Raw("@mail.ru")
    <br/>
    @Html.Raw("Дата для отчета")
    <br/>
    @Html.TextBoxFor(m=>m.Date)
    <input type="submit" value="Показать отчеты" />
}


@*<div>
        <table>
            @if (Model.Count != 0)
            {
                <tr>
                    <td width="20%">
                        @Html.Raw("Email")
                    </td>
                    <td width="80%">
                        @Html.Raw("ФИО")
                    </td>
                </tr>
            }
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.Raw(item.Email)
                    </td>
                    <td>
                        @Html.Raw(item.Name_1 + " " + item.Name_2 + " " + item.Name_3)
                    </td>
                    <td>
                        @Html.ActionLink("Изменить", "ChangeUser", "Home", new { @userId = item.User_Id }, null)
                        @Html.ActionLink("Удалить", "DeleteUser", "Home", new { @userId = item.User_Id }, null)
                    </td>
                </tr>
            }
        </table>
    </div>*@